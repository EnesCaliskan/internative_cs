name: internative_cs
description: Case Study assignment for Internative

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  hive: ^2.0.3
  hive_flutter: ^1.0.0
  google_fonts: ^1.1.2
  http:
  rxdart:
  provider:
  shared_preferences:


dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  mobx_codegen:
  flutter_lints: ^1.0.0
  hive_generator: ^1.0.1

flutter:
  uses-material-design: true
